/**
 * Package entry for `@photographic/voice`. The Vite client mounts from `main.ts`;
 * this file keeps the workspace export resolvable while the realtime surface is stubbed.
 */
export const voiceSurface = 'placeholder' as const;
