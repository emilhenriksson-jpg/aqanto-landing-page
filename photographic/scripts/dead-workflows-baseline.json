{
  "$comment": [
    "GitHub Actions workflows that exist in the repository but that GitHub never reads,",
    "because Actions only registers .github/workflows/ at the repository root. Read by",
    "scripts/check-workflow-locations.mjs, which fails on a new one and on a row here that",
    "no longer matches.",
    "",
    "Empty, and it should stay that way. The one row this list was created for --",
    "document-archive.yml, the off-site copy of every uploaded document, which had run zero",
    "times because it sat under the workspace directory -- has been moved to the repository",
    "root and is registered. Moving it also required a `defaults.run.working-directory` of",
    "`photographic`, since that path is resolved from the repository root no matter where",
    "the workflow file lives.",
    "",
    "A row here is a workflow that cannot fail, which is worse than one that fails loudly:",
    "it produces no red check for anyone to notice, so it reads exactly like a workflow that",
    "is passing. Prefer fixing the location over recording it."
  ],
  "known": []
}
